<%= form_for comment, :url => {  :controlle => :comments, :action => :show } do |f| %>
  <%= render 'shared/error_messages', resource: comment %>
  <%= f.hidden_field :blog_id %>
  <div class="field">
    <%= f.text_field :content, placeholder: "内容", class: "form-control"  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
